# contexto para outras IAs (arquivo local, fora do repo público por padrão)
CONTEXTO-IA.md

# SO / editores
Thumbs.db
desktop.ini
.DS_Store
*.log

# dependências (não deve haver — projeto é estático puro)
node_modules/

# segredos (não deve haver — projeto não usa credenciais)
.env
.env.*

# Cloudflare desativado - plataforma: Vercel
.wrangler/
.mf/
wrangler.toml
wrangler.json
wrangler.jsonc
.dev.vars

.dev.vars*
!.dev.vars.example
!.env.example
